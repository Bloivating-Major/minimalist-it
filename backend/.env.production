# Production Environment Configuration

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (Replace with your production MongoDB URI)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/todomaster?retryWrites=true&w=majority

# Frontend Configuration (Replace with your production domain)
FRONTEND_URL=https://your-frontend-domain.com

# CORS Configuration (Replace with your production domain)
CORS_ORIGIN=https://your-frontend-domain.com

# Authentication Configuration (Generate secure secrets for production)
JWT_SECRET=your-super-secure-jwt-secret-for-production-min-32-chars
SESSION_SECRET=your-super-secure-session-secret-for-production-min-32-chars

# Google OAuth Configuration (Replace with your production OAuth credentials)
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret
